# Production build: the SPA is served behind the nginx reverse proxy, which
# proxies /auth, /users, /friends, /photos, /ws, etc. to the backend on the
# same origin. Leave VITE_API_URL empty so requests stay same-origin.
VITE_API_URL=
VITE_DEV_AUTH=false
